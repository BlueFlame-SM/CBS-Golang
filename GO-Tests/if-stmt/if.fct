initialise-binding initialise-storing finalise-failing if-else
  (is-eq l-to-r
     (decimal-natural "1",
      decimal-natural "1"),
   scope
     (map
        ( ),
      sequential
        (print
           (decimal-natural "1"),
         null-value)),
   scope
     (map
        ( ),
      null-value))