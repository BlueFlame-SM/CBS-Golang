initialise-giving
initialise-binding
initialise-storing
multithread
give(
    channel-create(ints, 10),
    sequential(
      channel-send-else-wait(given, 13),
      print(channel-recv-else-wait given),
      null-type))

