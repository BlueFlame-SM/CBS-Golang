initialise-giving
initialise-binding
initialise-storing
multithread
give(
  channel-create(0),
  sequential(
    channel-send-else-wait(given, 0),
    print([]),
    channel-print(given),
    channel-receive-else-wait given))

